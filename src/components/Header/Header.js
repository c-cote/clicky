import React from "react";
import "./Header.css";

const Header = props => (

// banner area for game id  
  <header className="header">
    <h1>Inuyasha Clicky Game!</h1>
    <h2>
      Click on an image to earn points ...<br />
      but don't click on any more than once!
    </h2>
  </header>
);

export default Header;
